#!/usr/bin/env bash

set -Eeufo pipefail

echo --- :git: Testing if commit is an ancestor of the branch

git fetch origin "refs/heads/$BUILDKITE_BRANCH"

git show-ref

git merge-base --is-ancestor "$BUILDKITE_COMMIT" "refs/remotes/origin/$BUILDKITE_BRANCH"
